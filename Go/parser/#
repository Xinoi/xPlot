package parser

import (
	"github.com/gen2brain/raylib-go/raylib"
)

func Window() {
	rl.InitWindow(1200, 900, "xPlotGo")
	rl.SetTargetFPS(24)

	for !rl.WindowShouldClose(){

		rl.BeginDrawing()
		rl.ClearBackground(rl.Gray)

		rl.DrawCircle(600, 600, 200, rl.Gray)
		rl.EndDrawing()
	}
}
